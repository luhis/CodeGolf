@page
@model CodeGolf.Web.Pages.AdminModel
@{
    ViewData["Title"] = "Admin";
}

<h1 class="title">Admin</h1>

<section class="section">
    <div>
        <form asp-page-handler="ResetGame" method="post">
            <div class="field">
                <div class="control">
                    <button class="button is-warning">Reset Game</button>
                </div>
            </div>
        </form>
    </div>
</section>

